# MCP Builder 依赖
# 核心依赖（必需）
# 注：mcp库在服务器生成后使用，builder本身不需要

# 可选依赖 - SSE模式服务器需要
# fastapi>=0.100.0
# uvicorn>=0.23.0
# starlette>=0.30.0

# 开发依赖
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
mypy>=1.0.0
