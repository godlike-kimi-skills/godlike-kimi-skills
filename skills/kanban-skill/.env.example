# Kanban Skill 环境变量配置
# 复制此文件为 .env 并填写实际值

# 看板文件存储目录（默认为 ./kanban_boards）
KANBAN_DIR=./kanban_boards

# 默认看板列（用逗号分隔）
DEFAULT_COLUMNS=待办,进行中,已完成

# 日期格式
DATE_FORMAT=%Y-%m-%d

# 是否启用自动备份
ENABLE_AUTO_BACKUP=true
BACKUP_DIR=./kanban_backups
